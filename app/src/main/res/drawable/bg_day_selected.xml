<!-- 캘린더 그리드에서 "오늘은 아니지만 지금 골라서 보고 있는 날" 칸의 테두리 원. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="1.5dp"
        android:color="@color/brand_red" />
</shape>
